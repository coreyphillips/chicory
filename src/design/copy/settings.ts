/**
 * Settings, which may keep short text on screen (REDESIGN.md rule 2). Its
 * visible words still live in its screens; these are the ones it speaks.
 */
export const settings = {
  /** What a screen reader calls the Settings scene. */
  title: 'Settings',
};
